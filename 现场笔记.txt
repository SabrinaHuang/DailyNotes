
			
		/*  待验证 */	
	// 	react 本身融入 JS的API，比如XMLHttpRequest,navigator.geolocation等
	var React = require('react-native');
	var { Text } = React;
		*指定了三个对象
	var GeoInfo = React.createClass({
		getInitialState: function() {
		return { position: 'unknown' };
	},	
	
	componentDidMount: function() {
		navigator.geolocation.getCurrentPosition(
			(position) => this.setState({position}),
			(error) => console.error(error)
		);
	},
	
	render: function() {
	   return (
		  <Text>
			Position: {JSON.stringify(this.state.position)}
		  </Text>
		);
	   },
	});
	
	
	
	// react可以重用原生UI，方法是在RCTViewManager的子类里实现-(UIView *)view。需要调用时用RCT_EXPORT_VIEW_PROPERTY暴露。
	/* 研究重点 */
	//  OC代码
	#import "RCTViewManager.h"
	@interface MyCustomViewManager : RCTViewManager
	@end
	// 调用此view
	@implementation MyCustomViewManager
		- (UIView *)view
		{
			return [[MyCustomView alloc] init];
		}
		// 向JS暴露属性
		RCT_EXPORT_VIEW_PROPERTY(myCustomProperty);
	@end	
	
	// JS代码
	// 生成一个模型
	module.exports = createReactIOSNativeComponentClass({
		validAttributes: { myCustomProperty: true },
		uiViewClassName: 'MyCustomView',
	});
						
						
	布局总原则
		最外面的flex:1
		里面的不需要flex:1,只要指定flex方向即可
		justifyContent: 让控件在垂直方向上移动,运动终点的投影在垂直轴的中点
		alignItem: 让控件在水平方向上移动,运动终点的投影在水平轴的中点
		
	flex和所包含的控件的关系:
		默认情况下,container的内部是column排列,此时水平方向上flex
		
		改成row后,水平排列,竖直是flex
	
	npm 使用时,要到node_module文件夹下,然后npm install XXXXX --save,再在程序里调用
	npm install express
	然后express包就在node_modules目录下
	
	调用时
	import express from 'express';
	
	let是定义变量的方式
	let name = 'firstComponent';
	
	在JS里,nav被做成一个容器,控制页面的压栈和弹出.导航条的样式需要自行创建
		对navigator来
			写定义的地方,navigator控件在
				initialRoute方法里,存了当前活动页面的Route信息,包括name和component
				
				在renderScene回调里,
					renderScene={(route,navigatr)=>{………………}}里的route就包含这两个值,而navigator就是导航栏对象
				要渲染页面时,用route里的参数,并将navigator也传给新渲染的页面	
				
					let Component = route.component;
					<Component {...route.params} navigator={navigator} />
		
					
			在调用时,从props里获取navigator,用此nav来push
				const {navigator} = this.props;
				if(navigator){
					navigator.push(
						name:'secondComponent',
						component:SecondComponent,
					)
				}
				
			需要点击动作时,用touch组件包含需被调用的
				<TouchableOpacity>
				
				</TouchableOpacity>
				
				