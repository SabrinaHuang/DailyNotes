
			
		/*  待验证 */	
	// 	react 本身融入 JS的API，比如XMLHttpRequest,navigator.geolocation等
	var React = require('react-native');
	var { Text } = React;
		*指定了三个对象
	var GeoInfo = React.createClass({
		getInitialState: function() {
		return { position: 'unknown' };
	},	
	
	componentDidMount: function() {
		navigator.geolocation.getCurrentPosition(
			(position) => this.setState({position}),
			(error) => console.error(error)
		);
	},
	
	render: function() {
	   return (
		  <Text>
			Position: {JSON.stringify(this.state.position)}
		  </Text>
		);
	   },
	});
	
	
	
	// react可以重用原生UI，方法是在RCTViewManager的子类里实现-(UIView *)view。需要调用时用RCT_EXPORT_VIEW_PROPERTY暴露。
	/* 研究重点 */
	//  OC代码
	#import "RCTViewManager.h"
	@interface MyCustomViewManager : RCTViewManager
	@end
	// 调用此view
	@implementation MyCustomViewManager
		- (UIView *)view
		{
			return [[MyCustomView alloc] init];
		}
		// 向JS暴露属性
		RCT_EXPORT_VIEW_PROPERTY(myCustomProperty);
	@end	
	
	// JS代码
	// 生成一个模型
	module.exports = createReactIOSNativeComponentClass({
		validAttributes: { myCustomProperty: true },
		uiViewClassName: 'MyCustomView',
	});

              		关于网页的文字样式,例如环绕式
              		正文文字因为要贴边,所以一般不弄margin和padding,要弄都是float来弄margin和padding
              		
              		引用背景图时,用url("03.jpg")
              		
              		将body的背景色设为背景图片的边缘色,可以让背景图片和谐地融入背景中
              		
              		标题常需要较特殊的字体,用图片 替代文字
              		<h1><span>The title</span></h1>
              		
              		h1{
              			background-image:url("img/")
              		}
              		h1 span{
              			display:none;
              		}
              		
              		这样即可显示图片而隐藏文字
              		
              		九宫格制法
              			stpe1:
              				为body设置属性 textAlign:center
              				
              			step2:
              				为包含格子的容器设置属性margin:0 auto;
              				
              			step3:
              				设置格子属性
              				width:calc((100% - 6px) / 3)  
              				float: left;
              				height:120  
              				减去边框和margin的宽度,然后需要排几个格子,就除以几
              				
              				
              		#react native语法#
              			组件  extends Component{} 用大括号
              			
              			StyleSheet.create() 用小括号
              			所有的值都以{}包裹
              			
              			view
              				对嵌套的容器来说,外层容器可决定内层容器的排布方式,但无法决定它们的大小
              				如
				<View style={[styles.item,styles.lineLeftRight]}>
						<View style={[styles.flex,styles.center,styles.lineCenter]} >
							<Text>额2</Text>
						</View>
						<View style={[styles.flex,styles.center,styles.lineCenter]}>
							<Text>蛤2</Text>		
						</View>				
				</View>  
						外层容器决定了自己是平铺的,决定了自己的左右线宽
						内层容器决定了自己是平铺的,自己的内容是居中的,自己的底部线的宽度
              				
              	
				如果是封装的组件,最外层的容器的属性一般叫flex,需要设置到各边缘的margin,以及内部的对齐方式
				例如 alignItems:'center' 让item水平方向对齐
				
				列表组件的应用
					1 将数据依次封装到界面里
						// 定义
						var newsArr = [];
						// 生成组件并放入数组
						for( var i in this.props.news){
							var text = (
								<Text>
									{this.props.news[i]}
								</Text>
							);
							newsArr.push(text);
						}
						
					2 将数据展示到组件里
						return(
							<View>
								<Text>第一行是标题</Text>
								{newsArr}
							</View>
						)
						
					2  调用时
						import Array from './Component';
						
						<View>
							<Array news={['a','b','c','d','e','f','g',]} />
						</View>


					tabBarIOS的用法
						要设置诸如title, item, 用 property = {valueXX}即可.要注意语法较繁琐
						特别是内联函数
							
					引用图片的格式	
						<Image source={require('./image.png')} />
						