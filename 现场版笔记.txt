	#升级apache subversion的安装
		sh 命令用于执行脚本	

		要装一堆插件，蛋疼

		或者直接下载

		http://www.wandisco.com/subversion/download#osx

		然后在终端下设置
		$ cd

		$ vi .bash_profile

		在 .bash_profile文件中添加一行  export PATH=/opt/subversion/bin:$PATH，完后后更新下bash的环境变量

		$ source .bash_profile

		再次运行svn 命令

		$ svn --version


	#vi编辑器的常用命令
		刚打开时，按i 进入编辑状态
		切换到命令行，用 ':'
		保存并退出  wq
		如果是readonly 用wq!即可强行保存



	#mac下SVN重定位
		服务端地址更改时  本地库需要更改
		到本地库的根目录下，用 svn relocate 新的服务端地址 即可

	#路径访问
		在RN中，自定义模块一定要加上路径。如果不加路径，编译器会到node_modules里找


	#JS指定原生界面
		在appDelegate里，会通过RCTRootView桥接。而BundleURL就是被调用的JS资源的地址，

		形式为 [NSURL URLWithString:@"http://10.18.51.33:8081/index.ios.bundle?platform=ios&dev=true"];

		其中10.18.51.33:8081是开发服务器的地址， index.ios.bundle指定了所加载的起始页码


	#定时器
		用法：
			延时后执行任务
			// TimeOut是定时器
			var TimeOut = setTimeout(()=>{

			Geolocation.getCurrent(
			()=>{},
			(e)=>{});

			if( TimeOut ){
				clearTimeOut(TimeOut);
			}

			}, 5000);

	#定位和反定位
			看收藏的网站

			为何JSONstring时字典类型为 MDictonary?


	# ES5,ES6语法的差别
		在require里，ES5可以require('XXXpackage')来调用库，但ES6不行，得import Dimension, 然后 Dimension.get('window').width来调用


	#iOS里的 API
		访问通讯录
			AddressBook	只提供功能，UI要自己搭，叫做低级API
			AddressBookUI则提供了UI

	#通用知识  序列图
		生命线，一个特定实例的生命周期

		矩形窄条 -> 活动期（是函数体）

		消息 -> 用箭头表示	
				同步消息：等待接收方响应消息后，再继续动作

				异步消息：不用等回调




